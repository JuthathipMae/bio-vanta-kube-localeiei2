apiVersion: apps/v1
kind: Deployment
metadata:
  name: bio-web-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: bio-web
  template:
    metadata:
      labels:
        app: bio-web
    spec:
      containers:
      - name: bio-containers
        image: bio-containers:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3000
      